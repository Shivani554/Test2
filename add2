#include <stdio.h>

int main() {
    int x, y, sum = 0;
  
      // Read two numbers from the user
    printf("Enter two integers: ");
    scanf("%d %d", &x, &y);

    // Calculate the addition of a and b
    // using '+' operator
    sum = x + y;

    printf("Sum: %d", sum);

    return 0;
}

